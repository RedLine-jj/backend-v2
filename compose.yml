services:
  mysql:
    image: 'mysql:8'
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: redline
    ports:
      - '3306:3306'

  redis:
    image: 'redis:7-alpine'
    ports:
      - '6379:6379'
